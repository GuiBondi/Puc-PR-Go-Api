name: Go

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23'  # Update this to match the Go version specified in go.mod

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...

    # Deploy stage to Docker Hub
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: guibondi
        password: Melani2001@  # Ensure you set this secret in GitHub

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        push: true
        tags: guibondi/simpleapi:latest

    - name: Deploy to Docker for tests
      run: |
        docker pull guibondi/simpleapi:latest
        docker run -d -p 8080:8080 guibondi/simpleapi:latest
